{
  "name": "smoke-schedule",
  "version": "1.0.0",
  "description": "Fetches a weekly schedule from Marconi and displays in an interactive grid. Display with [smoke-schedule]",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Hackett",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "browser-sync": "^2.18.12",
    "gulp": "^3.9.1",
    "gulp-minify": "^1.0.0",
    "gulp-sass": "^3.1.0"
  }
}
